<div>
  <strong>Title:</strong>
  <%= display.title %>
  
  <% if display.contents.present? %>
    <% first_content = display.contents.first %>
    <br>
    <%= image_tag first_content.image_as_mini_thumbnail %>
  <% else %>
    <p>No contents present for the display.</p>
  <% end %>
</div>
